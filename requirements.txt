# Email Knowledge Pipeline — Local Dependencies

# ============================================
# LLM & Embeddings
# ============================================
openai>=1.10.0
tiktoken>=0.5.0
httpx>=0.27.0              # Timeout support for OpenAI client

# ============================================
# Document Processing
# ============================================
pypdf>=3.17.0
PyMuPDF>=1.23.0            # PDF structural analysis and classification
python-docx>=1.1.0
openpyxl>=3.1.2
python-pptx>=0.6.23
extract-msg>=0.48.0
python-magic>=0.4.27
xlrd>=2.0.1                # Legacy XLS support
striprtf>=0.0.26           # RTF parsing
beautifulsoup4>=4.12.0     # HTML parsing
pdf2image>=1.16.0          # PDF→image for Vision OCR (requires poppler)
msoffcrypto-tool>=5.0.0    # Decrypt encrypted Office files
# antiword: brew install antiword (mac) | apt install antiword (linux)
# pypff: needed for PST extraction (requires compilation)

# ============================================
# NLP & ML
# ============================================
spacy>=3.7.0
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
langdetect>=1.0.9
ftfy>=6.1.0                # Fix broken Unicode, mojibake, surrogates in email text
langid>=1.1.6              # Fallback language detection
python-dateutil>=2.8.2     # Date parsing
transformers>=4.36.0       # BART local summarization
torch>=2.1.0               # PyTorch backend for transformers
sentence-transformers>=2.2.0  # Local embeddings (all-MiniLM-L6-v2)

# spaCy models (install separately):
# python -m spacy download en_core_web_trf
# python -m spacy download nl_core_news_lg

# ============================================
# GraphRAG
# ============================================
igraph>=0.11.0
leidenalg>=0.10.0
networkx>=3.2.0
nano-vectordb>=0.0.4
graspologic>=3.3.0

# ============================================
# Evaluation
# ============================================
ragas>=0.1.0
datasets>=2.16.0

# ============================================
# Data Processing
# ============================================
pandas>=2.1.0
numpy>=1.26.0

# ============================================
# Utilities
# ============================================
pydantic>=2.5.0
python-dotenv>=1.0.0
tqdm>=4.66.0
tenacity>=8.2.0

# ============================================
# Azure Services
# ============================================
azure-storage-file-datalake>=12.17.0   # ADLS Gen2
azure-identity>=1.19.0
azure-search-documents>=11.6.0b5       # Azure AI Search
azure-cosmos>=4.7.0                    # Cosmos DB NoSQL
gremlinpython>=3.7.2                   # Cosmos DB Gremlin

# ============================================
# UI
# ============================================
gradio>=4.0.0
